class FirebaseConfig {
  // Enter your Firebase Realtime Database URL here (e.g. 'https://your-project-id-default-rtdb.firebaseio.com/')
  // If left empty or invalid, the app will automatically run in a fully-functional local simulator mode
  // where it simulates the database and logs locally via SharedPreferences, allowing full offline testing!
  static const String databaseUrl = ''; 
}
